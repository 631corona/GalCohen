
const initialState = {
    quarantinedSoldiers:
        [
            { name: "Carmel dahan", id: 1 },
            { name: "Gal Cohen", id: 2 }
        ]
}
const reducer = (state = initialState, action) => {
    return state;
};

export default reducer;